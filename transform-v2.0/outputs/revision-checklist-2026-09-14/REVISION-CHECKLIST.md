# Transform v2.0 · 修订清单（合并两轮评审）

**日期**：2026-09-14
**来源**：
- `outputs/usertest-2026-09-13/USERTEST-REPORT.md`（真实用户旅程测试，2026-09-13~14）
- `outputs/ux-review-2026-09-14/UX-JOURNEY-REVIEW.md`（全局用户旅程 + UIUX 专项，2026-09-14）

**前提**：保留现有修订（commit `8a566fe` 已上线）；假定用户**全程有 AI 支持**，本清单不涉及 AI 接入。
**口径**：每条修订均标注「问题 → 动作 → 可量化验收 → 成本」，验收标准必须能用数值或布尔断言判定，不接受"看起来更好了"。

---

## 0. 状态总览

| 状态 | 数量 | 说明 |
|---|---|---|
| ✅ 已闭环（不重复做） | 11 项 | 见 §1，来自 USERTEST §8 修复批，已 push 线上 |
| ✅ 批次 1–3 已完成 | 17 项 | R-01 ~ R-17 全部落地（见 §6 进度记录） |
| ✅ 批次 4 已完成 | 3 项 | R-18 / R-19 / R-20 走查完成，又修掉 4 个真实缺陷（见 §6） |
| ✅ R-22 命名统一 | 1 项 | 用户定名版（每日洞察 / 每日改变 / 成长资产 / 深入洞察 / 帮我改变），7 页落实（见 §6） |
| ✅ R-23 资产卡出口对调 | 1 项 | 成长资产卡两出口改为「深入洞察 → app」+「拿它去用 → 每日改变(简单版)」，并带认知起点（见 §6） |
| ✅ R-24 每日改变改形态 | 1 项 | 打卡机 → 改变任务清单：任务常显 · 7 天拆解常显 · 逐日打勾 · 每天记录「做了什么+产出物」；锚点只设一次（见 §6） |
| ⏸ 挂起（等中文验收） | 1 项 | R-21 英文镜像 —— 用户明确要求中文验收后再启动 |
| ⏸ 明确不做 | 3 项 | 见 §4 |

**一句话**：内容侧已达可发布水准（8.5），**外壳仍是原型**（信息架构 4.0 / 首屏价值 3.5 / 资产可用性 2.5）。
待修订项里**没有一条需要新功能**，全是"改名、并导航、重排首屏、加使用出口、别重置用户操作"。

**当前状态**：中文版 R-01 ~ R-20 + R-22 + R-23 + R-24 全部落地并通过 **311 项**自动验收；
R-21 英文镜像待 David 验收中文后启动。

---

## 1. 已闭环（不重复做）

来自 `USERTEST-REPORT.md §8`（commit `8a566fe`，已线上复验）：

| # | 缺陷 | 修复内容 | 状态 |
|---|---|---|---|
| BUG-1 | `连续 Infinity 天没发生` | `daysSinceLastDone()` 改为按实验开始日算 + `Number.isFinite` 兜底 | ✅ |
| BUG-2 | 洞察跳转丢失主题 | `app.html` 读取 `?topic=` 预填 `state.data.scenario` | ✅ |
| BUG-3 | 三脚手架输出互相覆盖 | `#qAiResult` 改累积式渲染（按 `aiHelps` 全量重绘） | ✅ |
| BUG-4 | 想透步按钮重叠 | `.q-action-row` 补 `display:flex + gap:16px + flex-wrap` | ✅ |
| BUG-5 | 同一步骤两个名字 | 首次引导文案统一为「摊开/想透/应用/沉淀」 | ✅ |
| P2-1 | 底部游离一行「先写一个锚点」 | 补 `punch.html` 缺失的 `#toast{}` 基础样式 | ✅ |
| P2-2 | UI 出现 emoji 🚀 | `punch.html` 完成图标改内联 SVG 对勾 | ✅（仅 punch，全站见 R-13） |
| P2-3 | 身份条「…的人」的人 | 新增 `idPhrase()` 判断尾字，不再重复追加 | ✅ |
| P2-4 | 沉淀步 AI 模板塞整句 | 改为通顺固定名词短语 | ✅ |
| P2-5 | 占位符含内部产品名 | `WorkBuddy` 全部替换为中性示例 | ✅ |
| 附加 | 主按钮配色不统一 | `think.html` 紫按钮改深绿 token | ✅ |

**回归基线**：`verify_v2.py 54/54` + `verify_p0.py 19/19` + `verify_p1.py 26/26` = **99/99** 全绿。
（`verify_p0` 尾部 404 = 浏览器隐式 favicon 请求，既有噪声，非缺陷。）

---

## 2. 待修订清单（21 项 / 4 批）

### 批次 1 · 体验骨架（P0）—— 一次改动消灭"废纸篓"第一印象

| ID | 问题 | 出处 | 动作 | 可量化验收 | 成本 |
|---|---|---|---|---|---|
| **R-01** | 页头与区块标题**逐字重复**（如「认知档案 / 每一次想清楚，沉淀出的长期认知资产」出现两次） | U-4 | 删除内容区重复标题块；`cognitive.html` / `behavior.html` 各一处 | 页面内该字符串出现次数 = **1** | 极低 |
| **R-02** | 资产页首屏可见资产 = **0 条**，且 4 区块中 2 个是垃圾桶 | U-3 §4.5 | 首屏重排：搜索框上第一屏 → 最近 3 条**露出正文** → 身份卡下移页脚 → 回收箱/已归档 0 项隐藏、有项降为页脚一行 | 首屏（900px）可见资产标题 ≥ **3**；可见垃圾桶区块 = **0** | 中 |
| **R-03** | 折叠卡只显示「场景 + 次数 + 日期」，扫不到内容 → "罗列感"根因（**M1**） | U-5 §4.4 | 改 `renderInsights` 模板：折叠态即露出洞察正文 1–2 行（大、深色），场景降为小灰字 | 折叠（未展开）状态下可见洞察正文行数 ≥ 1 | 极低 |
| **R-04** | 资产卡动作集 = `✏️编辑 / 🏷归类 / 📦归档 / 🗑删除`，**零使用动作** → 废纸篓根因（**M2**） | U-5 §4.4 | 洞察卡加主按钮「拿它去用 →」→ `app.html?from=<id>`；次按钮「用它开一个实验 →」→ `act.html?from=<id>`；管理动作收进「⋯」溢出菜单 | 展开洞察卡：使用类按钮 ≥ 2、管理类按钮**默认不可见**；点击后目标页读到 `?from=` | 低 |
| **R-05** | 新用户首屏读到 **5 句「还没有…」** | §3.6 | 合并为**统一首启引导块** + 一个主 CTA「开始想清楚 →」 | 空状态否定句数 5 → **1** | 低 |
| **R-06** | 筛选 chip 共 **11 个**（7 标签 + 4 时间），视觉重量超过内容 | §4.5 | 降为 3 个切换（全部 / 待用 / 常用）；标签筛选收进「筛选」面板 | 首屏可见 chip 数 ≤ **3** | 低 |

> **批次 1 完成即达成**：用户打开"我的成长资产"，第一屏就能读到自己的资产、并知道能拿它去用。这是清单里**投入产出比最高**的一批。

---

### 批次 2 · 连接（P1）—— 让资产真正被"用起来"

| ID | 问题 | 出处 | 动作 | 可量化验收 | 成本 |
|---|---|---|---|---|---|
| **R-07** | 资产无使用痕迹，用户看不到"我用了它"（**M3**） | §4.4 | 资产对象加 `uses:[{at,where}]`；卡片显示「用过 N 次 · 最近 M/D」；新增"按用得多"排序 | 使用一次后 `uses.length` +1，卡片文案同步 | 低 |
| **R-08** | 洞察/实验/模型是互不引用的孤岛（**M4**） | §4.4 | 双向显式关联：洞察卡 →「已变成 N 个实验 · 进行中 x/y 天」；实验卡 →「源于这条认知」；模型卡 →「由 N 条洞察归纳」 | 从洞察能一跳到达其产出实验，反向亦然 | 中 |
| **R-09** | 无回访触发，旧资产沉底（**M5**） | §4.4 | 首页「我发现了一个关于你的模式」下方，轻浮现 1 条旧资产（≥14 天未看 + 曾变成实验），不催促 | 满足条件时首页可见旧资产卡 = 1 条 | 低 |

> **批次 2 是价值主张的落地**：用户第一次看到"我 3 周前写的一句话 → 变成 7 天实验 → 现在第 4 天"，看到的是**认知→行为的因果链**，不是三条记录。这是全清单**最高价值的 AHA 放大器**。

---

### 批次 3 · 系统一致性（P1/P2）—— 消除"别扭感"与"不像高级 APP"

| ID | 问题 | 出处 | 动作 | 可量化验收 | 成本 |
|---|---|---|---|---|---|
| **R-10** | 同一件事 **5 个名字**（入口 A：认知提升/每日洞察/快速想清楚/伴我洞察/想清楚；入口 B 同理） | U-1 §3.1 | 全站统一为 **想清楚 / 做到**；资产父级「我的资产」→ 子「想法」「证据」；**只改显示名，不动文件名/href** | 每个动作全站显示名出现版本数 = **1** | 中 |
| **R-11** | **两套导航**且各页项数不同；「我的资产」不在任何内页导航 | U-2 §3.2 | 抽一套逐字相同的全局导航；「我的资产」进入导航；「存数据」移出顶栏；主题开关降级为图标 | 任一页顶栏 HTML 内容一致；内页可达「我的资产」 | 中 |
| **R-12** | 全站混用两种主绿（`#0E6B50` 与 `#1D9E75`），用户无法判断主操作 | U-7① §3.4 | `#1D9E75` 仅用于背景/进度/图表；**按钮底色只允许 `#0E6B50`**；改 punch「做到了」 | 全站 `#1D9E75` 出现在按钮底色的次数 = **0** | 低 |
| **R-13** | 约 **50–60 处**彩色 emoji（app 26 / behavior 24 / cognitive 24 / act 15…），与 SVG 图标风格撕裂 | U-7② §3.4 | 用现有内联 SVG 图标体系替换（想法→气泡、证据→对勾、编辑→铅笔、归类→标签、归档→盒子、删除→垃圾桶、时间→日历） | 正文彩色 emoji 检出数 = **0**（主题符号 ☾/☀ 与 ✓ 除外） | 中（量大但机械） |
| **R-14** | 一屏出现两个深绿实心元素，误判当前状态 | U-7③ §3.4 | 规则化：一个视口只允许一个深绿实心按钮 | 逐屏人工核对，违规 = 0 | 低 |
| **R-15** | 单卡展开高度实测 **1368px**（>1.5 屏） | §3.4 | 单卡展开 ≤ **600px**，超出用「展开更多」二次展开或子 tab | 任一卡展开高度 ≤ 600px | 中 |
| **R-16** | 展开态/筛选/滚动位置**不持久**，切筛选即全折叠、刷新即重置 → "不丝滑"技术根因 | U-6 §3.5 | 渲染前把展开态读入内存、渲染后回写；UI 状态写入 `trf_ui`；筛选 + 展开 + 滚动位置重载还原 | `setTagFilter` 后 `sectionOpen` 仍为 `true`；刷新后展开态还原；`trf_ui` 非空 | 中 |
| **R-17** | AI 调用等 20–30 秒只有静态等待；结果整块替换像"弹回去" | §3.5 | 改**分步进度**（"正在读你的 4 条信息 → 正在归类 → 正在写洞察"）；结果**就地展开追加**，保留上文 | 等待态出现分步文案；旧结果不被清除 | 中 |

---

### 批次 4 · 覆盖补测 + 英文镜像（收尾）

| ID | 问题 | 出处 | 动作 | 验收 | 成本 |
|---|---|---|---|---|---|
| **R-18** | `act.html`（做到·完整四步）未逐屏走通 | USERTEST §7 | 按真实用户身份逐屏走通，产出截图存证 | 全四步无断点，入档成功 | 中 |
| **R-19** | `cognitive.html` 标签筛选 / 洞察投票等 P1 功能未测 | USERTEST §7 | 逐项实测 + 修复发现的缺陷 | 功能可用、状态正确 | 中 |
| **R-20** | 移动端窄屏适配未测 | USERTEST §7 | 375px 窄屏走通主链路 | 无横向滚动、无元素压盖 | 中 |
| **R-21** | 英文镜像页滞后；`punch-en.html` 存在与中文版相同的 **toast 基础样式缺失** | USERTEST §8 备注 | 中文批次全部完成后，按逐页映射统一修订英文版；**务必带上 toast 修复** | 中英页面一一对应，R-01~R-17 全部同步 | 中 |

---

## 3. 验收与回归流程（每批次必须走）

1. 改前：`git status` 确认干净；必要时先建 `outputs/backup-YYYY-MM-DD/`。
2. 自测：`node --check` 语法检查（0 error）。
3. 回归：用带 playwright 的 venv 跑三套验收基线 ——
   `/Users/davidma/.workbuddy/binaries/python/envs/transform_verify/bin/python verify_v2.py`
   → `verify_v2(54) + verify_p0(19) + verify_p1(26) = 99/99` 必须全绿。
4. 实测：agent-browser 打开 `file://` 页面，对每条的**可量化验收**做断言（数值/布尔）。
5. 提交：描述性 commit message → push origin main → `curl` 线上冒烟。
6. 记录：截图存 `outputs/<批次>-YYYY-MM-DD/`，清单勾选状态更新至本文件。

---

## 4. 明确不做（避免误解为遗漏）

| # | 项 | 原因 |
|---|---|---|
| 1 | 首启引导加「连接 AI」步骤 | 已确认**假定用户全程有 AI 支持**，不新增该步骤以降低首启负担 |
| 2 | 引入后端 / 登录 | 架构结论：纯前端 localStorage 已满足当前需求 |
| 3 | 新增业务功能 | 本清单**零新功能**；全部为命名/导航/首屏/使用出口/状态持久化 |

---

## 5. 优先级速查（按投入产出比排序）

| 排序 | ID | 动作 | 收益 |
|---|---|---|---|
| 🥇 | R-03 | 折叠态露出洞察正文 | 检索从"不可用"→"可用"（改一个模板） |
| 🥈 | R-04 | 洞察卡加「拿它去用 →」 | 首次让认知资产有出口（复用 `act.html?id=` 范式） |
| 🥉 | R-12 | 单一绿 token | 消歧主操作（改配色） |
| 4 | R-02 | 资产首屏重排 | 消灭"废纸篓"第一印象 |
| 5 | R-01 | 删重复标题块 | 消除纯视觉噪声 |
| 6 | R-08 | 双向关联可见 | AHA 最大放大器 |
| 7 | R-10 | 命名收敛 | 消除"别扭"主因 |
| 8 | R-16 | 状态持久化 | "丝滑感"根因 |

---

## 6. 进度记录

### 2026-09-14 · 批次 1–3 完成（R-01 ~ R-17）

**结论**：中文版 17 项全部落地；英文版**按用户要求冻结**，等中文验收后再统一启动（R-21）。

**回归结果**（带 playwright 的 venv）：

| 验收套件 | 结果 |
|---|---|
| `verify_v2.py` | 54/54 |
| `verify_p0.py` | 19/19 |
| `verify_p1.py` | 26/26 |
| R-14 单屏深绿实心 ≤1（逐滚动位） | 9/9 |
| R-15 单卡展开 ≤600px（极值数据） | 4/4 |
| R-16 展开态/筛选/滚动持久化 | 10/10 |
| R-17 AI 分步进度 + 非破坏追加 | 10/10 |
| **合计** | **132 项全绿，0 JS 错误** |

**逐项证据（可量化）**：

| ID | 落地内容 | 量化证据 |
|---|---|---|
| R-10 | 全站统一为 想清楚 / 做到 / 我的资产 / 想法 / 证据；两个旧跳转页同步改名 | 旧名残留 = 0（grep） |
| R-11 | 6 个内页顶栏 HTML 逐字一致；「我的资产」入导航；存数据/AI 设置移入页脚；主题开关降为裸图标 | md5(顶栏, 忽略 active 标记) 单一值 |
| R-12 | 按钮底色只允许 `#0E6B50` | `#1D9E75` 出现在按钮底色 = 0 |
| R-13 | 彩色 emoji → 内联 SVG（24×24 统一图标体系） | 7 个中文页彩色 emoji = 0；`node --check` 7/7 OK |
| R-14 | 一屏只允许一个深绿实心元素 | 7 页 × 逐滚动位 = 9/9 通过 |
| R-15 | 折叠卡展开高度上限 600px（默认只展开最近一次 + 二级 `details`） | 极值数据下：认知 1368 → **536px**；行为 858 → **463px** |
| R-16 | 展开态/筛选/滚动写入 `trf_ui`，重渲染 + 刷新还原 | `setTagFilter` 后仍展开；刷新后还原；`trf_ui` 非空 = 10/10 |
| R-17 | AI 分步进度（真实条目数）+ 结果就地展开追加（不覆盖） | 进度文案含真实 N/M/K；结束后无残影；旧内容保留 + 「采用/保留」= 10/10 |

**R-17 设计取舍**：首次整合（字段为空）仍**直接填充**，不增加一次点击；
只有在**已有内容**时才展开「Transform 又给了一版」，并给出「采用这一版 / 保留我的」。
这样既满足「旧结果不被清除」，又不给新用户增加步骤（遵循「降低认知负荷」准则）。

### 2026-09-14 · 批次 4 完成（R-18 / R-19 / R-20）；R-21 按要求挂起

**R-21 状态**：⏸ **未启动**（用户明确要求「英文的先不要动，要等到中文修订验收后再启动」）。
中文版 20 项已全部落地，待 David 验收后再统一执行逐页映射。

**回归结果（累计 206 项全绿，0 JS 错误）**：

| 套件 | 结果 |
|---|---|
| `verify_v2.py` / `verify_p0.py` / `verify_p1.py` | 54 / 19 / 26 |
| R-14 单屏深绿实心 ≤1 | 9/9 |
| R-15 单卡展开 ≤600px（极值数据） | 4/4 |
| R-16 状态持久化 | 10/10 |
| R-17 AI 分步进度 + 非破坏追加 | 10/10 |
| R-18 act.html 全四步走查 | 11/11 |
| R-19 cognitive 筛选/使用/归档/回收箱/备份 | 28/28 |
| R-20 375px 窄屏 | 35/35 |
| **合计** | **206 项全绿** |

**本批修掉的真实缺陷（4 个，全部是走查中发现的）**：

| # | 缺陷 | 影响 | 修复 |
|---|---|---|---|
| B4-1 | `index.html` 顶栏需 477px，375px 屏上把**布局视口撑到 496px** | 移动端整页被缩小约 24%，文字变小 | 窄屏导航改两行（品牌+工具图标 / 三个主入口铺满），保留文字标签 |
| B4-2 | `app.html` `.topbar` 缺 `flex-wrap`，`gnav` 越界 110px（视口撑到 486px） | 同上；导航压住返回键 | 补 `flex-wrap: wrap`（与 punch.html 一致），gnav 三等分一行 |
| B4-3 | `act.html` `aiGeneratePlan()` 只写内存不落库 | AI 排好的 7 天计划，**刷新即丢** | 生成后立刻 `persistAction()` |
| B4-4 | 两处「无选择器的孤立 CSS 声明块」（app.html / act.html） | 无效 CSS，浏览器丢弃 | 删除孤立块（纯清理，无视觉变化） |

**命名补充修复（走查中发现，属 R-10 同类）**：
助手名在 index/think/act 仍为「知行」共 **28 处**（其中 think 15、act 11），而 app/cognitive/behavior 已是 **Transform**。
按此前已定的「Zhixing → Transform」口径统一，**7 个页面「知行」残留归零**。

**R-18 走查轨迹（截图存 `outputs/rev-b4-2026-09-14/r18_*.png`）**：
写下 → 拆解细分（AI 排 7 天）→ 行动锚定（早餐之后 / 身份）→ 反馈强化（第 1 天 + 身份票 1）→
走满 7 天自动毕业 → 产出（复盘）→ 入档成功（`archiveSuccess` + 按钮转「继续调整复盘」）→
「证据」页可读到（`已毕业 · 7/7`）。全程无断点。

**R-20 证据（截图存 `r20_375_*.png`）**：7 个页面 × {默认态, 扩展态} 均「无横向滚动 + 无可见内容越界 + 布局视口 = 375」。

### 2026-09-14 · R-22 命名统一（用户定名，中文 7 页）—— commit `e3db2f4`

**由来**：David 指出命名问题（顶栏不统一、对仗不工整「想清楚/做到」、资产页名字不对），并声明
「词语的定义由我来确定，但要统一」。

**定名表（v1 血缘 → v2 改前 → 用户定名）**：

| 概念 | v1 | v2（改前） | 用户定名 |
|---|---|---|---|
| 品牌对仗 | 想清楚，做得到 | 想清楚，做得到 | **保持** |
| 想清楚·轻入口 | 快速想清楚 | 想清楚 | **每日洞察** |
| 做到·轻入口 | 每日进步 | 做到 | **每日改变** |
| 想清楚·深入 | 伴我洞察 | 想清楚 · 深挖 | **深入洞察** |
| 做到·深入 | 伴我改变 | 做到 · 拆解 | **帮我改变** |
| 资产页 | 成长资产 | 我的资产 | **成长资产**（回到 v1） |

**执行口径（关键：区分「名字」与「动词」）**：
- **改**：`<title>` / 顶栏 / eyebrow / 入口按钮 / 跳转 CTA / 书名号「想清楚」「做到」当程序名时 → 共 **61 条规则**
- **不改**：动词用法（今天做到了 / 没做到 / 累计做到 / 把这次想清楚 / 想清楚一件事作句子成分）
- **不改**：步骤词（拆解目标 / AI 拆解 / 深挖）——只改入口标签，不动方法论词汇

**方法**：脚本化改（`outputs/verify-scripts/rename_naming.py`），每条规则**带计数断言**，
先 dry-run 断言全过再 `--apply`；改前整目录备份（`outputs/backup-before-naming-20260914-1806/`）。

**验收**：**250/250** 全绿、0 JS 错误；7 页线上 title 与顶栏逐字核对通过；`我的资产` 残留 = 0。

**顺带修掉的 3 处过期断言**（已用改名前备份复现，证明非本批引入）：

| 断言 | 失效原因 |
|---|---|
| b1 / b1b `R-01 重复标题串` | R-01 删除重复标题后，该文案又经 R-02/R-10 换代，断言里的旧串已不存在 |
| b2 `R-08d 实验卡显示「源于想法」` | R-15 把 `.a-source` 收进 `<details>` 折叠层，断言未展开折叠层故读到空串 |

**验收脚本已入库**：`outputs/verify-scripts/`（此前只存在 `/tmp`，重启即丢）。


### 2026-09-14 · R-23 成长资产卡出口「名字 ↔ 去处」对调

**由来**：David 指出资产卡上两个出口按钮**语义与行为都不对**：
「拿它去用」实际跑去了「深入洞察」，「用它开一个实验」跑去了「复杂改变版」。
他定名：**第一个按钮 = 深入洞察（去深入洞察页）· 第二个按钮 = 拿它去用（去每日改变的简单版）**。

**改法（cognitive.html + behavior.html 同一函数 `assetActions`）**：

| 位置 | 改前 | 改后 |
|---|---|---|
| 主按钮（solid） | 「拿它去用 →」→ `app.html?from=` | **「深入洞察 →」**→ `app.html?from=`（只换名，去处本就正确） |
| 次按钮 | 「用它开一个实验 →」→ `act.html?from=` | **「拿它去用 →」**→ `punch.html?insight=&nature=&scenario=` |
| 折叠头小药丸（仅 cognitive） | 「去用 →」→ `app.html?from=` | 「深入洞察 →」→ `app.html?from=`（同一去处，一名到底） |

**关键设计：复用既有通道，不新增机制**
「拿它去用」去的「每日改变」需要把这条想法当**认知起点**带过去，否则入口语义与行为不一致。
该通道**早已存在**——`think.html` 的「把这个洞察变成今天的改变 →」用的就是
`punch.html?insight=<洞察>&nature=<本质>&scenario=<场景>`，由 punch 的
`handoffInsight/handoffNature/handoffScenario` 承接（显示「认知起点」卡 + 预填输入）。
本次新增的只是 10 行纯函数 `punchHandoffUrl(obj)`，把资产对象映射到同一条查询串：
- `insight` = `essence.insight` → 回退 `essence.nature` → 回退 `reflection.learned`
- `nature` 仅在**与 insight 不同**时才带（避免起点卡出现两行重复）
- 全部为空时**退化为裸 `punch.html`**（不带空参数）

**顺带修掉**：
- `punch.html` 起点卡标签「来自**今日**洞察」→「来自你的想法」（旧名漏网 1 处；
  且该卡现在也能从成长资产跳来，写死来源已不准确）
- `app.html` / `act.html` 两处注释与事实不符（前者仍写「点『拿它去用』跳 app」，
  后者描述的入口已改投每日改变）→ 已同步

**验收**：新增 `outputs/verify-scripts/verify_r23.py`（**26/26**），
不只查文案，而是**真点一次**验证跳转后起点卡与预填都正确；全量回归 **276/276**、0 JS 错误。

**走查中发现的遗留（已记录、未改动，超范围且有风险）**：
`behavior.html`（成长资产 · 证据）的标记里**没有** `#insightList / #modelList / #tagFilters` 容器，
故其 `renderInsights()` / `renderModels()` / `renderTagFilters()` 在该页是**死代码**（`if(!box) return;` 直接返回），
`assetActions` 的 insight/model 分支在该页不可达。实测：该页只渲染行为实验卡 + 模式。
（该页的 `assetActions` 改动仍保留，以保持两页同函数一致；若该区块日后恢复即可直接生效。）


### 2026-09-14 · R-24 每日改变：从「打卡机」改为「改变任务清单」—— commit `cf72f48`

**由来（用户原话）**：有关每日改变，有大的设计问题 ——
我们把一个要改变的任务拆解成为 7 天的微行动，然后每天一个事情，然后每天繁琐的设定锚点，
更繁琐的是莫名其妙的又要说做到了，又要今天闭环、明天见，奇怪的证据。
事实上这是一个**改变的任务清单**，任何时候都需要看到自己输入的要改变的任务，以及拆解的 7 天微任务，
而且一天天的打勾完成（checklist），并且还要说自己在当天做的微任务和产出物，这样才能确定是一个每日改变。

**诊断**：不是细节问题，是**形态装错**。数据早就有了（`current.input` 要改变的任务、`current.plan[7]`
拆解、`current.logs` 完成记录），但主视图是「一天一张仪式卡」，四样该看见的全没显示；而
「做了什么 / 产出物」**根本没有字段**。

**用户拍板的四条口径**

| 议题 | 定案 |
|---|---|
| 锚点 | **必须保留，但不能成为负担** —— 7 天一次性设一个，只在创建时做一次；以后每天在这个锚点提示上做，且清单里每天提醒 |
| 每天记录 | 两个框：**今天我做了什么 + 产出物**（产出物可留空） |
| 保留的仪式 | 完成时的**轻**反馈 + 7 天走完的毕业总结（「身份确认」与独立「证据」按钮不保留） |
| 7 天走完 | **清单留存为记录 + 出口去复盘** |

**落地（punch.html 主视图重写）**

| 位置 | 改前 | 改后 |
|---|---|---|
| 主视图 | 一天一张深绿仪式卡 + 一个大按钮 | **改变任务清单**：任务头 + 7 段进度 + 7 天逐行打勾 |
| 要改变的任务 | 顶部被截断成一行 | 任务头常显（大字）+ 锚点 + 「第 N/7 天 · 已完成 N 天」 |
| 7 天拆解 | 只在创建时折叠看过一次 | **全部常显**在清单里，一次看全 |
| 打勾 | 「做到了」大按钮 | 今天行内「完成今天，打勾」（写了「做了什么」才可用） |
| 每日记录 | 无字段 | `logs[].note`（必填）+ `logs[].output`（可留空）；打勾后就地展开回看 |
| 已完成的行 | 无 | 打勾 + 点开可回看当天记录 |
| 完成反馈 | 「做到了」→ 弹大卡 → 「今天闭环·明天见」→「证据」 | 就地展开的轻反馈（行为证据 + 见证句），不再要求二次确认 |
| 底部出口 | 藏在反馈卡里 | 「想把这个改变做得更完整（含复盘与沉淀）？去完整行动页 →」 |
| 创建流程 | 4 步：定目标 / AI 拆解 / 选锚点 / 身份确认 | **3 步**：定目标 / AI 拆解 / **定锚点（一次，7 天通用）** |
| 「今天没做到」 | 虚线大按钮 | 清单内的轻链接 → 选因 → 当场给最小版（机制不变） |

**顺带修掉**
- 锚点措辞归一化 `anchorText()`：AI 给的锚点有的写「在早上打开电脑之后」、有的写「在我吃完午饭后」，
  模板再补一次会渲染成「在「**在**我吃完午饭后」之后做」。展示前统一去掉首「在」与尾「之后」。
- 反馈标题「第 X 天**做到了**！」→「第 X 天完成」（用户明确不接受「做到了」这个说法）。
- punch.html 错别字「早震」→「早上」（3 处：两处兜底锚点列表 + AI 提示示例）。
- 同日第二次打勾原本**静默吞掉用户输入**（新形态下清单会立刻推进到明天那一行，用户自然会接着打），
  改为明确提示「今天已经完成过一天了 —— 剩下几天明天接着来」，且不清空已输入内容。
  （「一天只记一次」的规则本身未改，属既有产品行为，已单独向用户说明待其定夺。）

**清理死代码**（随本次下线而失效）：`.cta-done` / `.cta-miss` / `.cta-ok` / `.tf-hint` / `.tx-*` 共 13 条样式、
`renderTodaySubs()`、`confirmDone()`、`hideTodayFeedback()`、goIdentityStage 的确认页。

**验收**：新增 `outputs/verify-scripts/verify_r24.py`（**35/35**，真跑浏览器 —— 清单形态 / 打勾推进 /
记录落库 / 回看 / 旧仪式下线 / 硬约束仍在 / 最小版支路 / 毕业态 / 锚点措辞）；
证据截图 `outputs/rev-r24-2026-09-14/`（清单默认态 · 填写态 · 打勾后回看 · 毕业态 · 窄屏 375）。
全量回归 **311/311**、0 JS 错误。

**三项硬约束未破**（既有验收依赖，迁移时逐条保住）：
`id="tfEvidence"` / `id="tfWitness"`（P0 身份票→证据）、「想把这个改变做得更完整」+`.tu-link`
（P1-W3 升级链接，带 goal + micro + fail）、`localFailTip`（无 AI key 的本地降级）。
故 `verify_v2` 54/54、`verify_p0` 19/19、`verify_p1` 26/26 全部保持绿。

**教训**
1. **结构替换必须用「函数级边界」而不是「下一个锚点」**：首版脚本用「renderTodaySubs 之后到
   renderPunchStepsBar 之前」做区间删除，一次误删了中间 23 个函数（304 行）。改用
   「内容里只含 1 个 `function `」+「必须含目标函数名」双断言后才安全。**区间删除前先断言区间内容特征。**
2. **改形态要连带检查「旧文案承载的机制」**：`tfEvidence`/`tfWitness`/`.tu-link` 这些 id 和类名背后挂着
   P0/P1 的验收，不能因为"卡片看起来是旧仪式"就整块删掉 —— 先查测试引用，再决定迁还是删。
3. **统一命名会让原本看不见的重复变得刺眼**（R-23 的教训），而**改形态会让原本看不见的行为暴露出来**：
   「同日只记一次」在一天一卡时不可见，变成清单后立刻暴露。**形态一改，要重跑一遍「边界操作」。**


## 7. UX-JOURNEY-REVIEW 三处缺口收尾（2026-09-16）
审计（audit_ux_report.py，38/38）发现报告点出但未进修订清单的 3 处缺口，已全部落地：
1. **§1.1-#2 首页「继续上次」**（index.html）：hero 主 CTA 下方新增一条 resume-bar。
   最多 1 条；优先级 ① 进行中且今天没打勾的改变实验 → punch.html ② 没想完的洞察草稿 → app.html。
   今天已打勾 / 什么都没有 → 完全不渲染（默认状态不强迫用户处理旧事务）。
2. **§1.1-#8 + §3.3 app 四步页右侧浮卡已移走**（app.html）：删除 #appAside 元素、renderAppAside
   函数及调用、.app-aside / .aside-* 全部样式，三栏 grid 改两栏（7 处断点），主内容区占满。
3. **§3.5-4 位置提示带「第 N 步」**（app.html）：四个 step-eyebrow 加序号
   （第 1 步 · 摊开 / 第 2 步 · 想透 / 第 3 步 · 应用 / 第 4 步 · 沉淀）。
验收：audit_ux_report.py 32/35 → **38/38**（新增 §1.1-#2a~d 四条状态断言）；
全量回归 **311/311** 不变。证据截图 outputs/ux-audit-2026-09-15/fix*.png（对照 gap*.png）。
排除项（David 口径）：AI Key 与命名一致性不在此列，命名由 David 定义。
